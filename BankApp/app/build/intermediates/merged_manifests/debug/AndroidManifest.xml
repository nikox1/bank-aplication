<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.nikodem.bankapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="34" />

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <permission
        android:name="pl.nikodem.bankapp.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="pl.nikodem.bankapp.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_bank"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_bank"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.BankApp"
        android:usesCleartextTraffic="true" >

        <!-- Login Activity (Main) -->
        <activity
            android:name="pl.nikodem.bankapp.activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.BankApp" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name="pl.nikodem.bankapp.activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.BankApp" />

        <!-- Dashboard Activity -->
        <activity
            android:name="pl.nikodem.bankapp.activities.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.BankApp" />

        <!-- Transfer Activity -->
        <activity
            android:name="pl.nikodem.bankapp.activities.TransferActivity"
            android:exported="false"
            android:theme="@style/Theme.BankApp" />

        <!-- BLIK Activity -->
        <activity
            android:name="pl.nikodem.bankapp.activities.BlikActivity"
            android:exported="false"
            android:theme="@style/Theme.BankApp" />

        <!-- History Activity -->
        <activity
            android:name="pl.nikodem.bankapp.activities.HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.BankApp" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="pl.nikodem.bankapp.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>